[project]
name = "py-examples"
version = "0.1.0"
description = "Repository of Python examples and patterns"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.128.0",
]

[tool.uv.workspace]
members = ["*"]
exclude = ["docli", "esearch", "typedsetting"]
